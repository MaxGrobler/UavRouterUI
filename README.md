# UavRouterUI

This is the front end for the routing project, it starts by sending waypoints plotted by the user to the backend. Once returned with a (possibly) new route it plotes this. it will also display drone information and if each drone can make the flight or not

# TO USE:

- Backend running

# TO RUN:
- run project from Program.cs